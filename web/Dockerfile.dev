# 前端开发环境 Dockerfile
FROM node:18-alpine

# 安装 pnpm
RUN npm install -g pnpm

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY package.json pnpm-lock.yaml ./

# 安装依赖
RUN pnpm install --frozen-lockfile

# 暴露端口
EXPOSE 5173

# 启动开发服务器
CMD ["pnpm", "dev", "--host"]
